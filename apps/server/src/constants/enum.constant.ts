export enum InteractionType {
  Undefined = 'undefined',
  Like = 'like',
  Comment = 'comment',
}

export enum InteractedOn {
  Undefined = 'undefined',
  Blog = 'blog',
  Comment = 'comment',
}

export enum OrderBy {
  CreatedAt = 'createdAt',
  UpdatedAt = 'updatedAt',
}

export enum Action {
  Create = 'create',
  Read = 'read',
  Update = 'update',
  Delete = 'delete',
}
